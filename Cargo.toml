[package]
name = "debaser"
version = "0.1.0"
edition = "2021"
description = "Deterministic release name generator"

[dependencies]
anyhow = "1"
clap = { version = "4.5", features = ["derive"] }
once_cell = "1"
